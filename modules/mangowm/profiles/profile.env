# Selected shared monitor profile for MangoWM.
# Supported values are the profiles declared in shared/wm/monitors.yaml.
# Use "auto" to select the best profile from currently connected Mango outputs
# when rendering during a live session; non-live renders fall back to "desk".

MANGO_MONITOR_PROFILE=auto
