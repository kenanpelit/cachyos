# Hand-maintained Mango window rule overrides.
#
# Canonical rules are generated from:
#   modules/mangowm/rules/window-rules.json
# into:
#   generated/window-rules.conf
#
# Keep this file for short-lived local exceptions only. Durable app behavior
# belongs in the manifest so validation and generated docs stay in sync.
