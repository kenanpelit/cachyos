[Unit]
Description=Delayed Flatpak managed installation
PartOf=graphical-session.target

[Timer]
# Grafik oturum gerçekten açıldıktan 5 dakika sonra tek seferlik başlat.
OnActiveSec=5min
AccuracySec=1min
Unit=flatpak-managed-install.service

[Install]
WantedBy=graphical-session.target
