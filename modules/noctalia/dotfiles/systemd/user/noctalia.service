[Unit]
Description=Noctalia Shell
# Hiçbir şeyi bekleme, hemen başla (en hızlı açılış için)
PartOf=graphical-session.target

[Service]
ExecStart=/usr/bin/qs -c noctalia-shell --no-duplicate
Restart=on-failure
RestartSec=1

[Install]
WantedBy=niri-session.target
