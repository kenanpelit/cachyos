description: Paru configuration (sudo pacman wrapper + user config)

dotfiles:
  - source: dotfiles/bin/pacman-sudo-paru
    target: ~/.local/bin/pacman-sudo-paru

post_install_hook: scripts/install.sh
post_hook_behavior: always
