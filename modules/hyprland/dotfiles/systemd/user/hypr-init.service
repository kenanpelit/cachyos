[Unit]
Description=Hyprland session bootstrap (monitors + audio)
Wants=pipewire.service wireplumber.service
After=graphical-session.target hyprland-session.target
PartOf=hyprland-session.target
ConditionEnvironment=WAYLAND_DISPLAY
ConditionEnvironment=XDG_CURRENT_DESKTOP=Hyprland

[Service]
Type=oneshot
TimeoutStartSec=60
Environment=PATH=/usr/local/bin:/usr/bin:%h/.local/bin:%h/bin
Environment=HYPR_BOOT_DELAY=5
ExecStart=/usr/bin/bash -lc 'sleep "${HYPR_BOOT_DELAY:-5}"; if [[ -x "%h/.local/bin/hypr-osc" ]]; then "%h/.local/bin/hypr-osc" init; elif command -v hypr-osc >/dev/null 2>&1; then hypr-osc init; else echo "[hypr-init] hypr-osc not found; skipping"; fi'
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=hyprland-session.target
