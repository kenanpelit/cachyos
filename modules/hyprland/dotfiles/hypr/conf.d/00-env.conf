# Hyprland compositor-local overrides.
#
# Shared session environment now lives in:
#   ~/.config/environment.d/10-hyprland.conf
#
# Keep this file for compositor-only `env=` directives or host-local debugging
# toggles that should not leak into the wider session environment.
