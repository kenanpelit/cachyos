auto_detect_launch_prefix = true
before_load = []
git_on_demand = true
ignored_providers = ['bitwarden', 'dnfpackages']
launch_prefix = 'uwsm app --'
overload_local_env = false
terminal_cmd = 'kitty'

[provider_hosts]
