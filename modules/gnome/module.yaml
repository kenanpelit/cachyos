description: GNOME Desktop (minimal config) ported from nixosc

dotfiles:
  - source: scripts/gnome_tty
    target: ~/.local/bin/gnome_tty

post_install_hook: scripts/setup-gnome.sh
post_hook_behavior: ask

# NOTE: The nixosc GNOME module is mostly dconf settings.
# On Arch/CachyOS, apply those via gsettings/dconf (manual or custom script).
