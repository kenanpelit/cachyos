# Intel Meteor Lake iGPU tuning
#
# These are the same values we enforce via GRUB. Keeping them here as well makes
# the desired runtime state explicit and provides a fallback when i915 is loaded
# as a module after boot.
options i915 enable_guc=3 enable_fbc=1 enable_dc=0 enable_psr=0
