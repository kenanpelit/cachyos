# Linux Meteor runtime overrides.
#
# `-net.core.default_qdisc` is intentionally tolerant: kernels built without
# CONFIG_NET_SCHED do not expose the sysctl, but BBR-capable kernels should use
# fq when the key exists.
-net.core.default_qdisc = fq
net.ipv4.tcp_congestion_control = bbr
