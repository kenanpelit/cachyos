[Unit]
Description=Delayed Stasis (Idle Manager)
PartOf=graphical-session.target

[Timer]
# Delay relative to the graphical session becoming active, not user-manager start.
OnActiveSec=5s
AccuracySec=1s
Unit=stasis.service

[Install]
WantedBy=graphical-session.target
