#!/usr/bin/env bash
set -euo pipefail

if [ -x "${HOME}/.local/bin/gnome_tty" ]; then
  exec "${HOME}/.local/bin/gnome_tty"
fi

if command -v gnome_tty >/dev/null 2>&1; then
  exec gnome_tty
fi

exec gnome-session --session=gnome --no-reexec
