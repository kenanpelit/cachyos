#!/usr/bin/env bash
set -euo pipefail

# Legacy shim: keep stale desktop entries working, but always route through the
# UWSM-first Hyprland session wrapper.
exec /usr/local/bin/hyprland-uwsm-session "$@"
