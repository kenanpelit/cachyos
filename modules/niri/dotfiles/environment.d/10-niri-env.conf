# Niri Session Specific Environment
# Session identity lives in the UWSM wrapper + session init helpers. Shared
# PATH/browser/runtime defaults live in 00-wayland.conf.

MPV_NIRI_WIDTH=640
MPV_NIRI_HEIGHT=360
MPV_NIRI_MARGIN_X=32
MPV_NIRI_MARGIN_Y=96

# Icons (shared cursor defaults come from 00-wayland.conf)
XDG_ICON_THEME=kora
ICON_THEME=kora
