// Niri 26.04 background effects. Keep this conservative: xray blur is cheap,
// while non-xray blur is still experimental and intentionally not enabled.

blur {
  passes 2
  offset 3
  noise 0.01
  saturation 1.15
}

// Integrate static and Noctalia-backed wallpapers into the overview backdrop.
layer-rule {
  match namespace=r#"^(wallpaper|noctalia-wallpaper-.+|noctalia-wallpaper-video-.+|noctalia-parallax-.+)$"#
  place-within-backdrop true
}

// Noctalia shell surfaces already use compositor blur where supported. These
// rules cover panels/popups that rely on the compositor-side background effect.
// Keep overview/launcher overlays out of this set: when opened from the
// four-finger overview gesture they can cover the visible workspaces and make
// the whole overview look like a blurred blank layer.
layer-rule {
  match namespace=r#"^noctalia-(popup|popupmenu|control|control-center|settings|session-menu|osd|selection-menu).*$"#

  geometry-corner-radius 14

  background-effect {
    blur true
  }

  popups {
    geometry-corner-radius 12

    background-effect {
      blur true
    }
  }
}

// The native Niri overview should stay inspectable. Disable blur explicitly for
// companion overview/launcher surfaces even if they request it themselves.
layer-rule {
  match namespace=r#"^noctalia[-:]niri-overview-launcher.*$"#
  match namespace=r#"^noctalia:(niri-overview-launcher).*$"#
  match namespace=r#"^noctalia-launcher-overlay.*$"#

  background-effect {
    blur false
  }

  popups {
    background-effect {
      blur false
    }
  }
}

// Screenshot/recording overlays must stay crisp and unblurred.
layer-rule {
  match namespace=r#"^(noctalia-region-selector|noctalia-record|noctalia-measure|noctalia-annotate|noctalia-mirror|noctalia-pin|noctalia-shell:regionSelector)$"#

  background-effect {
    blur false
  }
}
