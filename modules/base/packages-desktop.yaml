packages:
  # Desktop utilities (moved nemo, rofi)
  - grim
  - slurp
  - wf-recorder
  - swappy
  - brightnessctl
  - extra/tigervnc
  - remmina
  - gnome-monitor-config-git

  # Documents (moved evince, zathura)
  - poppler
  # pdftk pulls java-runtime-headless; pick a concrete provider to avoid pacman prompts
  - jdk-openjdk
  - pdftk
  - qalculate-gtk

  # Security
  - keepassxc
  - age
  # sops moved to packages.yaml
  - pwgen

  # Flatpak (optional but matches previous setup)
  - flatpak
